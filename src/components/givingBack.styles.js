import { css } from '@emotion/react';

const styles = css`
  .pushing-diversity {
    margin-left: auto;
  }

  .badges-container {
    text-align: right;
  }

  .diversity-header-section {
    position: relative;
  }

  .title-right {
    text-align: right;

    img {
      margin-bottom: -1rem;
      margin-right: 45px;
    }
  }
`;

export default styles;

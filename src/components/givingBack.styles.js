import { css } from '@emotion/react';

const styles = css`
  .pushing-diversity {
    margin-left: auto;
  }

  .badges-container {
    display: flex;
  }

  .diversity-header-section {
    h3 {
      display: inline-block;
    }
  }
`;

export default styles;

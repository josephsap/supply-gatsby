import { css } from '@emotion/react';

const styles = css`
  .pushing-diversity {
    margin-left: auto;
  }

  .diversity-header-section {
    h3 {
      display: inline-block;
    }
  }
`;

export default styles;

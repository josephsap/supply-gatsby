import { css } from '@emotion/react';

const styles = css`
  .footer-container {
    list-style: none;
    padding-left: 0;
  }

  .footer-item {
    display: inline-block;
    margin-right: 3rem;
    vertical-align: top;

    img {
      width: 19px;
      height: 19px;
    }
  }
`;

export default styles;

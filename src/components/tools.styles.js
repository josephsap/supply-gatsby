import { css } from '@emotion/react';

const styles = (theme) => css`
  .tools-header-border {
    border: 3px solid;
    text-align: center;
    border-radius: 70px;
  }

  .tools-description {
    font-size: 3.2rem;
    line-height: 4.2rem;
  }
`;

export default styles;
